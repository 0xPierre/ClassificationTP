//
// Created by adrien on 12/20/23.
//

#include "Settings.h"

void playMusic(char title[]) {
   /* Mix_ResumeMusic();
    if (Mix_PlayingMusic()) {
        return;
    }
    char filePath[50];
    if (strcmp(title, "writing") == 0) {
        strcpy(filePath, "../Assets/Sounds/brush.wav");
    }

    if (!strcmp(filePath, "")) {
        return;
    }*/

    //Mix_Music * music = Mix_LoadMUS(filePath);
    //Mix_PlayMusic(music, 0);
}

void stopMusics() {
 /*   if (!Mix_PlayingMusic()) {
        return;
    }
    Mix_HaltMusic();*/
}